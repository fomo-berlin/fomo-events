{
  "name": "fomo-berlin-events",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node build-from-json.js",
    "build:notion": "node build.js",
    "geo": "node scripts/enrich-geo.js",
    "dev": "npm run build && (cd public && python3 -m http.server 8765)"
  }
}
